name: bytebeam
description: ByteBeam - Fast offline P2P file transfer
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  file_picker: ^8.0.3
  path_provider: ^2.1.2
  network_info_plus: ^6.1.4
  permission_handler: ^11.3.1
  archive: ^3.4.10
  uuid: ^4.3.3
  device_info_plus: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
```

**Only one change — but it's the critical one:**
```
BEFORE:  network_info_plus: ^5.0.3  ❌ very outdated, causes conflicts
AFTER:   network_info_plus: ^6.1.4  ✅ stable, compatible with everything
